<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All recommends
    </h1>

    <a href="/recommends/new" class="btn btn-block btn-outline-secondary">
      Add a new recommend
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Song
        </th>

        <th>
          User
        </th>

        <th>
          Activity
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @recommends.each do |recommend| %>
      <tr>
        <!-- Display recommend.id start -->
        <td>
          <%= recommend.id %>
        </td>
        <!-- Display recommend.id end -->

        <!-- Display recommend.song_id start -->
        <td>
          <% if recommend.song.present? %>
            <a href="/songs/<%= recommend.song.id %>">
              <%= recommend.song.artist %>
            </a>
          <% end %>
        </td>
        <!-- Display recommend.song_id end -->

        <!-- Display recommend.user_id start -->
        <td>
          <% if recommend.user.present? %>
            <a href="/users/<%= recommend.user.id %>">
              <%= recommend.user.username %>
            </a>
          <% end %>
        </td>
        <!-- Display recommend.user_id end -->

        <!-- Display recommend.activity_id start -->
        <td>
          <% if recommend.activity.present? %>
            <a href="/activities/<%= recommend.activity.id %>">
              <%= recommend.activity.activity_name %>
            </a>
          <% end %>
        </td>
        <!-- Display recommend.activity_id end -->

        <!-- Display recommend.created_at start -->
        <td>
          <%= time_ago_in_words(recommend.created_at) %> ago
        </td>
        <!-- Display recommend.created_at end -->

        <!-- Display recommend.updated_at start -->
        <td>
          <%= time_ago_in_words(recommend.updated_at) %> ago
        </td>
        <!-- Display recommend.updated_at end -->

        <td>
          <a href="/recommends/<%= recommend.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @recommends, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
